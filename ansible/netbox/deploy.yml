---
- name: Deploy Netbox
  hosts: netbox_servers
  become: true
  gather_facts: true

  roles:
    - common
    - postgresql
    - redis
    - netbox
    - nginx

# Post deploy:
# cd /opt/netbox
# source venv/bin/activate
# cd netbox
# python3 manage.py createsuperuser
