# Grafana Ansible Role

This role installs and manages Grafana on target hosts.

## Variables
- `grafana_version`: Version of Grafana to install (default: latest)
- `grafana_port`: Port for Grafana server (default: 3000)
