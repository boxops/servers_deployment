all:
  children:
    servers:
      children:
        dhcp_servers:
          hosts:
            kea_dhcp_lab:
              ansible_host: 10.0.1.10
              ansible_user: ubuntu
              ansible_become_password: "{{ vault_sudo_password }}"  # Use ansible-vault for production
              ansible_ssh_private_key_file: ~/.ssh/id_rsa
  vars:
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
