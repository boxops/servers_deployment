all:
  children:
    devices:
      hosts:
        tachyon-lab:
          ansible_host: 10.0.1.10
          ansible_user: admin
          ansible_become_password: "{{ vault_sudo_password }}"  # Use ansible-vault
